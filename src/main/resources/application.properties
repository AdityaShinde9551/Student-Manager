# ==============================
# App Config
# ==============================
spring.application.name=student-manager
server.port=8080

# ==============================
# Supabase PostgreSQL Database
# ==============================
spring.datasource.url=jdbc:postgresql://db.brtrigsgzauvemxdyfgz.supabase.co:5432/postgres?user=postgres&password=STDManager@123
spring.datasource.username=postgres.brtrigsgzauvemxdyfgz
spring.datasource.password=STDManager@123

# ==============================
# JPA / Hibernate
# ==============================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# "update" = adds new columns (marks fields) without deleting existing data
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ==============================
# Thymeleaf
# ==============================
spring.thymeleaf.cache=false
